# Where the new implementation is allowed to disagree with the frozen oracle.
#
#   <output>  <file>  <reason>
#
# One line per fixed defect per file. A difference that is not listed fails the
# run, and so does a line whose difference has gone away or was never reached.


# Text and attributes are escaped. `--ram <MB>` in the documentation used to
# reach the browser as a tag and be swallowed; now it is visible.
html  bare-metal.md            text and attributes are escaped
html  changelog.md             text and attributes are escaped
html  codegen.md               text and attributes are escaped; h4 is a heading, not a paragraph of four hashes
html  db.md                    text and attributes are escaped
html  http-demos.md            text and attributes are escaped
html  language-reference.md    text and attributes are escaped
html  lsp.md                   text and attributes are escaped
html  memory-model.md          text and attributes are escaped
html  packages.md              text and attributes are escaped
html  patterns.md              text and attributes are escaped
html  reserved-names.md        text and attributes are escaped
html  stdlib-reference.md      text and attributes are escaped
html  tour.md                  text and attributes are escaped
html  tutorial-rest-api.md     text and attributes are escaped
html  tutorial-type-inference.md text and attributes are escaped
html  tutorial.md              text and attributes are escaped
html  fence_and_escape.md      text and attributes are escaped; h4 is a heading, not a paragraph of four hashes

# Both of these are the same defect: the two line-oriented outputs had no
# idea what a fenced code block was, so they applied markdown rules to code.
text  bare-metal.md            code inside fences is left as written; a fence marker line becomes empty
text  changelog.md             code inside fences is left as written; a fence marker line becomes empty
text  codegen.md               code inside fences is left as written; a fence marker line becomes empty
text  db.md                    code inside fences is left as written; a fence marker line becomes empty
text  http-demos.md            code inside fences is left as written; a fence marker line becomes empty
text  language-reference.md    code inside fences is left as written; a fence marker line becomes empty
text  lsp.md                   code inside fences is left as written; a fence marker line becomes empty
text  memory-model.md          code inside fences is left as written; a fence marker line becomes empty
text  patterns.md              code inside fences is left as written; a fence marker line becomes empty
text  reserved-names.md        code inside fences is left as written; a fence marker line becomes empty
text  stdlib-reference.md      code inside fences is left as written; a fence marker line becomes empty
text  tour.md                  code inside fences is left as written; a fence marker line becomes empty
text  tutorial-redis-client.md code inside fences is left as written; a fence marker line becomes empty
text  tutorial-rest-api.md     code inside fences is left as written; a fence marker line becomes empty
text  tutorial-type-inference.md code inside fences is left as written; a fence marker line becomes empty
text  tutorial.md              code inside fences is left as written; a fence marker line becomes empty
text  fence_and_escape.md      code inside fences is left as written; a fence marker line becomes empty
text  fence_unterminated.md    code inside fences is left as written; a fence marker line becomes empty

toc   changelog.md             lines inside fenced code blocks are no longer counted as headings
toc   codegen.md               lines inside fenced code blocks are no longer counted as headings
toc   db.md                    lines inside fenced code blocks are no longer counted as headings
toc   http-demos.md            lines inside fenced code blocks are no longer counted as headings
toc   tutorial-rest-api.md     lines inside fenced code blocks are no longer counted as headings
toc   tutorial.md              lines inside fenced code blocks are no longer counted as headings
toc   fence_and_escape.md      lines inside fenced code blocks are no longer counted as headings
